<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        {{!-- <link href="css/normalize.css" rel="stylesheet" media="all"> --}}
        <link href="/css/style.css" rel="stylesheet" media="all">
        <title>Engineering UK - {{pageTitle}}</title>
        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    </head>
    <body class="careers eng-careers">
        {{> header}}
        <div class="career-finder-module">
            <div >
                <h3>Select focus area...</h3>
                <ul>
                    {{#each careers}}
                        <li><a href="#" onclick="toggleSub(&#39;Environment&#39;, this); return false;">{{name}}</a></li>
                    {{/each}}
                </ul>
                {{!-- <button class="purple-btn" onclick="goForSearch()" >Help me find my career &gt;</button> --}}
            </div>
            <div class="eng-filters">
                <nav id="top-nav" role="navigation">
                    <button class="menu-text menu-toggle-switch">
                        Menu<span></span>
                    </button>
                    <ul class="top-layer-nav-list">
                        <li><a href="#">Resource Type</a>
                            <ul class="menu_330">
                                {{#each resourcetypes}}
                                    <li><a href="/about-us/overview-for-employers/">{{name}}</a></li>
                                {{/each}}
                            </ul>
                        </li>
                        <li><a href="#">Age Group</a>
                            <ul class="menu_330">
                                {{#each agegroups}}
                                    <li><a href="/about-us/overview-for-employers/">{{name}}</a></li>
                                {{/each}}
                            </ul>
                        </li>
                        <li><a href="#">Distance Away</a>
                            <ul class="menu_330">
                                {{#each distances}}
                                    <li><a href="/about-us/overview-for-employers/">{{name}}</a></li>
                                {{/each}}
                            </ul>
                        </li>
                    </ul>
                </nav>
            </div>
            <div class="career-rip"></div>
        </div>
        <div class="article-column eng-article-column">
            {{#each articles}}
                {{> article}} 
            {{/each}}
        </div>  
        {{> footer}}

        <script>
            //nav slide
            $(document).ready(function () {
                $(".menu-text").click(function () {
                    $(".top-layer-nav-list").slideToggle(200);
                });
                // This animated the toggle.
                if ($('.target').is(':visible')) { }
            });
            $('#top-nav li ul').hide().removeClass('fallback');
            $('#top-nav li').click(
            function () {
                $('#top-nav li ul').not($(this).children("ul").slideToggle(200)).hide();
            }
        );
        </script>

    </body>
</html>